{
  "name": "world-explorer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "topojson-client": "^3.1.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "world-atlas": "^2.0.2"
  }
}
